apiVersion: v1
kind: Service

metadata:
  name: nginx-service
  namespace: cicd-demo

spec:

  selector:
    app: nginx-demo

  ports:

  - port: 80
    targetPort: 80
    nodePort: 30080

  type: NodePort